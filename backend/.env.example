DATABASE_URL="postgresql://user:password@host:5432/qa_pulse?schema=public"
JWT_SECRET="generate-with: node -e \"console.log(require('crypto').randomBytes(64).toString('hex'))\""
PORT=3001

# CORS (comma-separated origins, e.g. https://your-app.onrender.com)
CORS_ORIGIN="http://localhost:5173"

# SMTP Configuration
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_SECURE="false"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM=""

# App URL (used in email links - your frontend URL)
APP_URL="http://localhost:5173"
