{
  "name": "qa-pulse-backend",
  "version": "1.0.0",
  "description": "QA Pulse backend API",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "npx prisma generate && npx tsc && cd ../frontend && npm install --include=dev && npm run build",
    "build:backend": "npx tsc",
    "start": "NODE_ENV=production node dist/server.js",
    "db:migrate": "npx prisma migrate dev",
    "db:seed": "tsx src/seed.ts",
    "db:reset": "npx prisma migrate reset --force",
    "db:generate": "npx prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "@types/bcryptjs": "^2.4.6",
    "@types/cors": "^2.8.18",
    "@types/express": "^5.0.2",
    "@types/express-rate-limit": "^5.1.3",
    "@types/jsonwebtoken": "^9.0.9",
    "@types/node": "^22.15.3",
    "@types/nodemailer": "^8.0.0",
    "@types/uuid": "^10.0.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^4.21.2",
    "express-rate-limit": "^8.4.1",
    "jsonwebtoken": "^9.0.2",
    "nodemailer": "^8.0.7",
    "prisma": "^5.22.0",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3",
    "uuid": "^11.1.0"
  },
  "devDependencies": {
    "ts-node": "^10.9.2"
  }
}
